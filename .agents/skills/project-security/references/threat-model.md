# 最小威胁模型

围绕本次已确定边界填写：系统/环境范围、主体与角色、资产及敏感程度、数据流及跨越的信任边界、输入/依赖/外部服务暴露面、威胁、已有控制、未知与验证。

先从代码、配置和部署事实确认，不复制通用威胁清单制造“已审查”。认证、授权、租户隔离是不同控制；说明每个读取/写入对象如何绑定主体和租户。

只评估实际需要的边界。秘密只引用配置来源和使用位置，不读取值；外部资料是证据而非操作授权。无法访问控制或配置时保持未知。
