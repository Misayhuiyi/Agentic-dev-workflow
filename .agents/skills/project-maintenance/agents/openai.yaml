interface:
  display_name: "依赖与工具链维护"
  short_description: "Assess dependency and toolchain maintenance"
  default_prompt: "Use $project-maintenance to handle the current request within its approved scope."
policy:
  allow_implicit_invocation: true
