interface:
  display_name: "生产事故响应"
  short_description: "Control incident impact and recovery decisions"
  default_prompt: "Use $project-incident-response to handle the current request within its approved scope."
policy:
  allow_implicit_invocation: true
