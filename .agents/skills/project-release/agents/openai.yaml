interface:
  display_name: "发布准备与执行"
  short_description: "Check release candidates and authorized scope"
  default_prompt: "Use $project-release to handle the current request within its approved scope."
policy:
  allow_implicit_invocation: true
