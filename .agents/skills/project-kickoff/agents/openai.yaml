interface:
  display_name: "项目建项与首次接入"
  short_description: "Establish project scope and first milestone"
  default_prompt: "Use $project-kickoff to handle the current request within its approved scope."
policy:
  allow_implicit_invocation: true
