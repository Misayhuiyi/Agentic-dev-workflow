interface:
  display_name: "项目文档维护"
  short_description: "Maintain authoritative project documentation"
  default_prompt: "Use $maintaining-project-docs to handle the current request within its approved scope."
policy:
  allow_implicit_invocation: true
