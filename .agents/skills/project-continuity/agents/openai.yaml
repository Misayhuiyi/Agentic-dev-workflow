interface:
  display_name: "任务恢复与交接"
  short_description: "Recover task facts and freshness at handoff"
  default_prompt: "Use $project-continuity to handle the current request within its approved scope."
policy:
  allow_implicit_invocation: true
